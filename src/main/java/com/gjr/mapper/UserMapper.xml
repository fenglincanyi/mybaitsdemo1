<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.gjr.mapper.UserMapper">
    <insert id="insertUser" parameterType="com.gjr.po.User">
        INSERT INTO userinfo(username, password) VALUES (#{username}, #{password})
    </insert>
    <update id="updateUser" parameterType="com.gjr.po.User">
        UPDATE userinfo set username=#{username}, password=#{password} WHERE id=#{id}
    </update>
    <delete id="deleteUser" parameterType="integer">
        DELETE FROM userinfo WHERE id=#{id}
    </delete>
    <select id="findUserById" parameterType="integer" resultType="com.gjr.po.User">
        SELECT * FROM userinfo WHERE id=#{id}
    </select>
</mapper>